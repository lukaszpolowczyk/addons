{
  "name": "addons-page",
  "version": "1.0.0",
  "description": "Łukasz Polowczyk",
  "homepage": "https://lukaszpolowczyk.github.io/addons",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rsync -av --progress . build --exclude build --exclude node_modules --exclude .git --exclude package-lock.json --exclude package.json --exclude .gitignore --exclude .vscode",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^1.1.0"
  }
}
